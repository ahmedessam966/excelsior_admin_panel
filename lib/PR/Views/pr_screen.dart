import 'package:flutter/material.dart';

class PublicRelationsScreen extends StatelessWidget {
  const PublicRelationsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
